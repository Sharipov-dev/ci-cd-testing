{
  "name": "server",
  "version": "1.0.0",
  "description": "Express API server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "model:gen": "npx sequelize-cli model:generate",
    "migration:gen": "npx sequelize-cli migration:generate",
    "postfix:cjs": "bash -lc 'for f in models/*.js; do [ -f \"$f\" ] && mv \"$f\" \"${f%.js}.cjs\"; done; for f in migrations/*.js; do [ -f \"$f\" ] && mv \"$f\" \"${f%.js}.cjs\"; done; for f in seeders/*.js; do [ -f \"$f\" ] && mv \"$f\" \"${f%.js}.cjs\"; done;'"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}

